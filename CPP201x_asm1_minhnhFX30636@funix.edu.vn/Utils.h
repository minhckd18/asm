#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <string>
#include <sstream>
#include <regex>

using namespace std;

void readInt(istream& is, int& num, const int& max, const int& min = 0);

#endif // UTILS_H
